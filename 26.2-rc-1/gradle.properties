# Important Notes:
# Dependency versions come from the version catalog (com.iamkaf.platform)
# Custom resource properties must be added to the core convention plugin expandProps model.

# Project
project.version=4.0.0+26.2-rc-1
project.group=com.iamkaf.liteminer
project.java=25
project.build-java=25
project.minecraft=26.2-rc-1
project.plugins=2.1.2
# Forge and NeoForge artifacts are unavailable for this RC. Re-enable them when loader artifacts exist.
project.enabled-loaders=fabric

# Mod Information
mod.name=Liteminer
mod.authors=iamkaf
mod.id=liteminer
mod.license=MIT
mod.credits=
mod.description=A veinmining mod.

# Version Ranges
mod.minecraft-range=>=26.2-rc.1
mod.fabric-range=>=26.2-rc.1
mod.forge-loader-range=[62,)
mod.neoforge-loader-range=[4,)

# Gradle
org.gradle.jvmargs=-Xmx4G
org.gradle.daemon=false
org.gradle.parallel=true

# Publishing
publish.dry-run=false
publish.curseforge.id=1133225
publish.modrinth.id=VTnHoofC
publish.release-type=release
publish.game-versions=26.2
dependencies.modrinth.required=amber,konfig
dependencies.curseforge.required=amber-lib,konfig
environments.client=required
environments.server=required
